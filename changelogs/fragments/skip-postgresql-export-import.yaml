---
minor_changes:
  - Add ``artifact_skip_postgres`` to skip PostgreSQL dumps on export and restores on import (set independently per playbook run). Manifest records ``database.has_dumps`` so dump-less artifacts auto-skip restore. Import warns when dumps are present but restore is explicitly skipped.
