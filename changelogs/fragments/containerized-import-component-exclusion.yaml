---
minor_changes:
  - Add `artifact_import_controller`, `artifact_import_eda`, and `artifact_import_hub` variables to allow excluding individual components from a containerized import (gateway is always imported).
