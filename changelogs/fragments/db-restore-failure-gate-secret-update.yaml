---
bugfixes:
  - >-
    Fix containerized import continuing into secret rotation and container
    removal when the database restore fails - re-raise the failure from the
    postgresql db_import block so the outer consume.yml rescue restarts
    services and aborts instead of leaving nodes in a split state.
